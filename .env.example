# Slack Bot Token (xoxb-...) — from OAuth & Permissions → Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Slack App-Level Token (xapp-...) — from Basic Information → App-level tokens
# Requires connections:write scope
SLACK_APP_TOKEN=xapp-your-app-token-here

# Channel to watch (optional — bot responds to @mentions everywhere regardless)
# Set to restrict message responses to a specific channel
SLACK_CHANNEL_ID=C0AJBKTR0JU

# LLM Provider
ANTHROPIC_API_KEY=sk-ant-your-key-here
